# https://dart.dev/guides/libraries/private-files
# Created by `dart pub`
.dart_tool/

# Avoid committing pubspec.lock for library packages; see
# https://dart.dev/guides/libraries/private-files#pubspeclock.
pubspec.lock

.idea
.local-chrome

# Local scratch data (e.g. saved requisition dumps) used while running examples
*.data

# FVM Version Cache
.fvm/

# Node deps for the brand-asset generator (tool/). The rendered PNGs under
# .github/assets are committed; their node_modules are not.
tool/node_modules/